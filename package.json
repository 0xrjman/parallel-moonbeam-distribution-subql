{
  "name": "prallel-moonbeam-distribution-subql",
  "version": "1.0.0",
  "description": "subquery diction for moonbase-alpha and ...",
  "main": "dist/index.js",
  "scripts": {
    "build": "./node_modules/.bin/subql build",
    "codegen": "./node_modules/.bin/subql codegen",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
    "dev": "./node_modules/.bin/subql codegen && ./node_modules/.bin/subql build && docker-compose pull && docker-compose up --remove-orphans",
    "prepack": "rm -rf dist && npm run build",
    "test": "jest",
    "ipfs:publish": "./node_modules/.bin/subql publish"
  },
  "homepage": "https://github.com/rjman-ljm/parallel-moonbeam-distribution-subql",
  "repository": "github:rjman-ljm/parallel-moonbeam-distribution-subql",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Ryan",
  "license": "Apache-2.0",
  "resolutions": {
    "ipfs-unixfs": "6.0.6"
  },
  "dependencies": {
    "ethers": "^5.7.2",
    "@ethersproject/bytes": "^5.7.0",
    "@polkadot/api-augment": "^9.13.4",
    "@polkadot/types": "^9",
    "moonbeam-types-bundle": "^2.0.7"
  },
  "devDependencies": {
    "@polkadot/api": "^9",
    "@subql/cli": "1.11.0",
    "@subql/types": "1.7.1",
    "@subql/frontier-evm-processor": "0.1.2",
    "@terra-money/terra.js": "^3.0.8",
    "tslib": "^2.5.0",
    "typescript": "4.3.2"
  },
  "exports": {
    "chaintypes": "src/chaintypes.ts"
  }
}